/**
 * 環境変数検証スクリプト（User App）
 *
 * predev/prebuild フックから自動実行され、環境変数のバリデーションを実行します。
 * バリデーションエラー時は、プロセスを停止してdev/build実行を防ぎます。
 *
 * @see design.md セクション3.2.2: ビルド前検証スクリプト
 */

import "@/lib/env"; // env.ts をインポートしてバリデーション実行

// バリデーション成功時は何も表示せず、エラー時のみ env.ts で例外がスローされる
