# Design Document

## 概要

Admin Appの本番ビルド失敗を修正するための技術設計です。`simple-error-test/page.tsx` が Next.js App Router のレイアウト要件を満たしていないため、`[locale]/simple-error-test/page.tsx` に移動し、既存の locale layout を継承させることで問題を解決します。

### 目標

- ✅ Next.js App Router のレイアウト要件を満たす正しいファイル配置
- ✅ 本番ビルド（`npm run build`）を成功させる
- ✅ i18n 構造との一貫性を保つ
- ✅ 既存機能への影響を最小限に抑える

### スコープ

**対象範囲**:
- `frontend/admin-app/src/app/simple-error-test/page.tsx` のファイル移動
- 本番ビルドの検証
- CI/CD パイプラインでのビルド成功確認

**対象外**:
- ファイル内容の変更（移動のみ）
- User App の修正（User App は影響を受けない）
- テストコードの追加（既存テストは維持）

## アーキテクチャ分析

### 現在の構造（問題あり）

```
frontend/admin-app/src/app/
├── [locale]/
│   ├── layout.tsx           # ✅ locale 用レイアウト（HTML構造 + NextIntlClientProvider）
│   ├── page.tsx             # ✅ ホームページ（/ja, /en）
│   └── test-error/          # ✅ エラーテストページ（/ja/test-error, /en/test-error）
│       └── page.tsx
├── simple-error-test/       # ❌ レイアウトなし
│   └── page.tsx             # ❌ Next.js App Router 要件違反
├── global-error.tsx         # ✅ グローバルエラーハンドラ
└── layout.tsx               # ✅ ルートレイアウト（metadata のみ、HTML構造なし）
```

**問題の詳細**:

1. **Next.js App Router の要件違反**:
   - `app/simple-error-test/page.tsx` はアプリルート直下にある
   - `app/layout.tsx` は存在するが、HTML構造（`<html>`、`<body>`）を含まない
   - Next.js は各ページに対して完全な HTML 構造を持つレイアウトを要求する
   - 開発サーバー（`next dev`）は緩い検証、本番ビルド（`next build`）は厳密に検証

2. **i18n 構造との不一致**:
   - 他のページ（`page.tsx`、`test-error/page.tsx`）は `[locale]/` 配下にある
   - これらのページは `/ja/xxx` と `/en/xxx` でアクセス可能
   - `simple-error-test` のみがルート直下にあり、一貫性がない

### 提案する構造（修正後）

```
frontend/admin-app/src/app/
├── [locale]/
│   ├── layout.tsx           # ✅ locale 用レイアウト（HTML構造 + NextIntlClientProvider）
│   ├── page.tsx             # ✅ ホームページ（/ja, /en）
│   ├── simple-error-test/   # ✅ [locale] 配下に移動
│   │   └── page.tsx         # ✅ locale layout を継承、i18n 対応
│   └── test-error/          # ✅ エラーテストページ
│       └── page.tsx
├── global-error.tsx         # ✅ グローバルエラーハンドラ
└── layout.tsx               # ✅ ルートレイアウト（metadata のみ）
```

**修正による改善**:

1. **Next.js App Router 要件を満たす**:
   - `[locale]/simple-error-test/page.tsx` は `[locale]/layout.tsx` を継承
   - `[locale]/layout.tsx` が `<html>` と `<body>` を提供
   - 本番ビルドが成功する

2. **i18n 構造との一貫性**:
   - 他のページと同じ `[locale]/` 配下に配置
   - URL: `/ja/simple-error-test`、`/en/simple-error-test`
   - `NextIntlClientProvider` を自動的に継承

3. **後方互換性**:
   - ファイル内容は変更なし
   - 既存の Error Boundary テスト機能は維持
   - 他のページへの影響なし

## 主要な設計決定

### 決定 1: ファイル移動のみ、内容変更なし

**理由**:
- `simple-error-test/page.tsx` のコード自体に問題はない
- Client Component として正しく実装されている
- `ApiError` の throw ロジックは Error Boundary テストとして適切
- URL が `/simple-error-test` から `/ja/simple-error-test` に変わるが、これはテストページなので影響なし

**実装方法**:
```bash
# Git を使ったファイル移動（履歴を保持）
git mv frontend/admin-app/src/app/simple-error-test/page.tsx \
       frontend/admin-app/src/app/[locale]/simple-error-test/page.tsx
```

**代替案（却下）**:
- ❌ **Option A**: アプリルートに `layout.tsx` を追加 → 不要な複雑化、i18n との不一致
- ❌ **Option C**: ファイル内容を変更してエラー検証を調整 → 問題の本質（配置）と関係ない

### 決定 2: [locale] ディレクトリ配下に配置

**理由**:
- プロジェクトは next-intl を使用した i18n 対応アーキテクチャ
- すべてのページが `[locale]/` 配下にある規約
- `[locale]/layout.tsx` が以下を提供:
  - HTML 構造（`<html lang={locale}>`、`<body>`）
  - `NextIntlClientProvider` による多言語対応
  - Geist フォントの CSS 変数設定

**トレードオフ**:
- ✅ **利点**: Next.js 要件準拠、一貫性、i18n 自動統合
- ⚠️ **注意**: URL が変わる（`/simple-error-test` → `/ja/simple-error-test`）
  - ただし、これはテストページなので外部リンクや SEO への影響なし

### 決定 3: ディレクトリ構造を作成

**理由**:
- `[locale]/simple-error-test/` ディレクトリを作成
- 将来的に `simple-error-test` 関連の追加ファイルを配置可能
- Next.js のファイルベースルーティング規約に準拠

**実装方法**:
```bash
mkdir -p frontend/admin-app/src/app/[locale]/simple-error-test
git mv frontend/admin-app/src/app/simple-error-test/page.tsx \
       frontend/admin-app/src/app/[locale]/simple-error-test/page.tsx
```

### 決定 4: 旧ディレクトリの完全削除

**理由**:
- `src/app/simple-error-test/` ディレクトリは空になる
- 空のディレクトリを残すと混乱を招く
- Git は空のディレクトリを追跡しないため、自動的に削除される

**検証方法**:
```bash
# 移動後、旧ディレクトリが存在しないことを確認
ls -la frontend/admin-app/src/app/simple-error-test 2>/dev/null
# → "No such file or directory" が正しい結果
```

## コンポーネント仕様

### 1. SimpleErrorTestPage

**ファイルパス（修正前）**: `frontend/admin-app/src/app/simple-error-test/page.tsx`
**ファイルパス（修正後）**: `frontend/admin-app/src/app/[locale]/simple-error-test/page.tsx`

**責務**: Error Boundary のテストページ（意図的に ApiError を throw）

**実装内容（変更なし）**:
```typescript
"use client";

import { ApiError } from "@shared/api-error";
import type { RFC7807Problem } from "@/types/errors";

export const dynamic = "force-dynamic";

export default function SimpleErrorTestPage() {
  const problem: RFC7807Problem = {
    type: "http://localhost/errors/test",
    title: "Simple Test Error",
    status: 400,
    detail: "This is a simple test error thrown directly",
    error_code: "TEST-001",
    trace_id: "test-trace-id-12345",
    instance: "/simple-error-test",
    timestamp: new Date().toISOString(),
  };

  const error = new ApiError(problem);
  throw error; // Error Boundary でキャッチされる
}
```

**特徴**:
- Client Component（`"use client"` ディレクティブ）
- `force-dynamic` により毎回動的レンダリング
- RFC 7807 準拠の `ApiError` を throw
- Console ログなし（シンプル版）

**レイアウト継承（修正後）**:
- `[locale]/layout.tsx` から以下を継承:
  - `<html lang={locale}>` タグ
  - `<body>` タグと Geist フォント CSS 変数
  - `NextIntlClientProvider` による i18n コンテキスト

### 2. LocaleLayout（既存、変更なし）

**ファイルパス**: `frontend/admin-app/src/app/[locale]/layout.tsx`

**責務**: locale 用のレイアウト（HTML 構造と i18n プロバイダー）

**主要機能**:
- 動的ロケールパラメータ（`ja`、`en`）の受け取り
- `getMessages({ locale })` による多言語メッセージ取得
- `NextIntlClientProvider` で子コンポーネントに i18n コンテキスト提供
- Geist Sans/Mono フォントの CSS 変数設定

**修正後の影響**:
- `simple-error-test/page.tsx` が自動的にこのレイアウトを継承
- URL: `/ja/simple-error-test`、`/en/simple-error-test` でアクセス可能
- Error Boundary で表示されるエラーメッセージも多言語化される（`NextIntlClientProvider` 経由）

## テスト戦略

### 1. 本番ビルド検証

**目的**: 修正後に本番ビルドが成功することを確認

**手順**:
```bash
cd frontend/admin-app

# TypeScript 型チェック
npm run type-check
# → エラーなし

# ESLint
npm run lint
# → 新規エラー・警告なし

# 本番ビルド
npm run build
# → ✓ Compiled successfully
# → .next/ ディレクトリに成果物生成

# 本番サーバー起動
npm run start
# → Ready on http://localhost:13002
```

**成功基準**:
- TypeScript エラーなし
- ESLint エラー・警告なし
- ビルドエラーなし
- `.next/` ディレクトリに静的ファイル生成
- サーバー正常起動

### 2. ページアクセス検証

**目的**: 修正後のページが正しく表示されることを確認

**手順**:
```bash
# 本番サーバー起動後
# ブラウザで以下にアクセス

# 日本語版
http://localhost:13002/ja/simple-error-test
# → Error Boundary が表示される
# → エラーメッセージが日本語で表示される

# 英語版
http://localhost:13002/en/simple-error-test
# → Error Boundary が表示される
# → エラーメッセージが英語で表示される
```

**成功基準**:
- Error Boundary が正しく表示される
- エラーメッセージが locale に応じた言語で表示される
- Console に ApiError のログが出力される（`error-handling-pattern` spec の仕様）

### 3. 既存テストの実行

**目的**: 修正が既存機能を破壊していないことを確認

**手順**:
```bash
cd frontend/admin-app

# Jest テスト実行
npm test
# → すべてのテストがパス
```

**成功基準**:
- 既存の Jest テストがすべてパス
- テストカバレッジが維持される（修正前と同等）
- 新規テストコード追加は不要（ファイル移動のみのため）

### 4. リグレッションテスト

**目的**: 他のページへの影響がないことを確認

**検証対象**:
- ✅ User App のビルド（影響を受けない）
- ✅ Admin App の他のページ（`page.tsx`、`test-error/page.tsx`）
- ✅ Admin App の既存 E2E テスト

**手順**:
```bash
# User App のビルド
cd frontend/user-app
npm run build
# → 成功（Admin App の変更が影響しない）

# Admin App の他のページへのアクセス
# http://localhost:13002/ja → ホームページ正常表示
# http://localhost:13002/ja/test-error → エラーテストページ正常表示
```

**成功基準**:
- User App のビルド成功
- Admin App の他のページが正常に表示される
- 既存 E2E テストがパス

## マイグレーション戦略

### Phase 1: ファイル移動（5分）

**タスク**:
1. ディレクトリ作成: `frontend/admin-app/src/app/[locale]/simple-error-test/`
2. Git を使ったファイル移動:
   ```bash
   git mv frontend/admin-app/src/app/simple-error-test/page.tsx \
          frontend/admin-app/src/app/[locale]/simple-error-test/page.tsx
   ```
3. 旧ディレクトリ削除確認:
   ```bash
   rmdir frontend/admin-app/src/app/simple-error-test 2>/dev/null || true
   ```

**検証**:
- 新しいファイルパスが存在する
- 旧ディレクトリが存在しない
- Git 履歴が保持される（`git log --follow`）

### Phase 2: ローカル検証（10分）

**タスク**:
1. TypeScript 型チェック: `npm run type-check`
2. ESLint: `npm run lint`
3. 本番ビルド: `npm run build`
4. 本番サーバー起動: `npm run start`
5. ブラウザアクセス:
   - `/ja/simple-error-test`
   - `/en/simple-error-test`

**検証**:
- すべてのコマンドがエラーなく完了
- Error Boundary が正しく表示される
- エラーメッセージが多言語化される

### Phase 3: コミットとプッシュ（5分）

**タスク**:
1. Git コミット:
   ```bash
   git add frontend/admin-app/src/app/[locale]/simple-error-test/page.tsx
   git commit -m "Fix: 🔧 Admin App本番ビルド失敗修正（simple-error-test/page.tsx → [locale]配下に移動）

   - Next.js App Routerレイアウト要件を満たすため、simple-error-test/page.tsx を [locale]/simple-error-test/page.tsx に移動
   - [locale]/layout.tsx を継承し、HTML構造とNextIntlClientProviderを自動取得
   - URL変更: /simple-error-test → /ja/simple-error-test, /en/simple-error-test
   - i18n構造との一貫性を確保
   - 本番ビルド成功を確認

   Close: #124

   🤖 Generated with [Claude Code](https://claude.com/claude-code)

   Co-Authored-By: Claude <noreply@anthropic.com>"
   ```
2. ブランチ作成とプッシュ:
   ```bash
   git checkout -b fix/124/simple-error-test-layout-fix
   git push -u origin fix/124/simple-error-test-layout-fix
   ```

**検証**:
- コミットが作成される
- ブランチがリモートにプッシュされる

### Phase 4: Pull Request 作成（5分）

**タスク**:
1. GitHub CLI で PR 作成:
   ```bash
   gh pr create --title "Fix: 🔧 Admin App本番ビルド失敗修正（simple-error-test/page.tsx構造問題）" \
                --body "$(cat <<'EOF'
   ## Summary
   - Admin Appの本番ビルド失敗を修正（Next.js App Routerレイアウト要件違反）
   - `simple-error-test/page.tsx` を `[locale]/simple-error-test/page.tsx` に移動
   - `[locale]/layout.tsx` を継承し、HTML構造とi18n対応を自動取得
   - URL変更: `/simple-error-test` → `/ja/simple-error-test`, `/en/simple-error-test`
   - i18n構造との一貫性を確保

   ## Test plan
   - [x] TypeScript型チェック成功（`npm run type-check`）
   - [x] ESLint成功（`npm run lint`）
   - [x] 本番ビルド成功（`npm run build`）
   - [x] 本番サーバー起動成功（`npm run start`）
   - [x] ブラウザアクセス確認（`/ja/simple-error-test`, `/en/simple-error-test`）
   - [x] Error Boundary正常表示確認
   - [x] エラーメッセージ多言語化確認
   - [ ] CI/CDビルド成功確認（PR作成後）

   Close: #124

   🤖 Generated with [Claude Code](https://claude.com/claude-code)
   EOF
   )"
   ```

**検証**:
- PR が作成される
- Issue #124 にリンクされる

### Phase 5: CI/CD 検証（自動）

**タスク**:
- GitHub Actions が自動実行される
- フロントエンドテストワークフロー:
  - TypeScript 型チェック
  - ESLint
  - Jest テスト
  - （Issue #127 実装後）本番ビルド

**検証**:
- CI/CD パイプラインがすべて成功
- PR がマージ可能な状態になる

### ロールバック計画

**問題が発生した場合**:
```bash
# ファイルを元の場所に戻す
git mv frontend/admin-app/src/app/[locale]/simple-error-test/page.tsx \
       frontend/admin-app/src/app/simple-error-test/page.tsx

# コミットを取り消す
git reset --soft HEAD~1

# ブランチを削除
git checkout main
git branch -D fix/124/simple-error-test-layout-fix
```

**リスク評価**:
- 🟢 **低リスク**: ファイル移動のみ、内容変更なし
- 🟢 **影響範囲**: Admin App の1ページのみ
- 🟢 **テストページ**: 外部リンクや SEO への影響なし

## 要件トレーサビリティ

### Requirement 1: Next.js App Routerレイアウト構造の修正

| Acceptance Criteria | 設計での対応 | 検証方法 |
|---------------------|------------|---------|
| 1.1 本番ビルド成功 | Phase 2: `npm run build` 実行 | ビルドエラーなし |
| 1.2 [locale]/layout.tsx 継承 | `[locale]/simple-error-test/page.tsx` に移動 | レイアウト継承確認 |
| 1.3 URL: /ja/simple-error-test, /en/simple-error-test | Next.js のファイルベースルーティング | ブラウザアクセス確認 |
| 1.4 ルートレイアウト要件エラーなし | `[locale]/layout.tsx` が HTML 構造を提供 | ビルドエラーなし |
| 1.5 旧ディレクトリ削除 | Phase 1: `rmdir` で確認 | `ls` で存在しないこと確認 |

### Requirement 2: 本番ビルドの検証とテスト

| Acceptance Criteria | 設計での対応 | 検証方法 |
|---------------------|------------|---------|
| 2.1 ビルドプロセス完了 | Phase 2: `npm run build` | `.next/` ディレクトリ生成 |
| 2.2 本番サーバー起動 | Phase 2: `npm run start` | ポート13002で起動 |
| 2.3 /ja/simple-error-test アクセス | Phase 2: ブラウザアクセス | Error Boundary 表示 |
| 2.4 /en/simple-error-test アクセス | Phase 2: ブラウザアクセス | 英語版 Error Boundary 表示 |
| 2.5 TypeScript 型チェック | Phase 2: `npm run type-check` | エラーなし |
| 2.6 ESLint | Phase 2: `npm run lint` | エラー・警告なし |

### Requirement 3: CI/CDパイプラインとの整合性

| Acceptance Criteria | 設計での対応 | 検証方法 |
|---------------------|------------|---------|
| 3.1 PR 作成で自動実行 | Phase 4: `gh pr create` | GitHub Actions 自動実行 |
| 3.2 ファイル変更でワークフロートリガー | `.github/workflows/` の paths 設定 | CI/CD ログ確認 |
| 3.3 npm run build 成功検証 | Phase 5: CI/CD 自動実行 | CI/CD ステータス確認 |
| 3.4 型エラー検証 | Phase 5: CI/CD 自動実行 | CI/CD ステータス確認 |
| 3.5 Issue #127 実装後の自動実行 | 将来対応（本 PR 対象外） | Issue #127 で実装 |

### Requirement 4: ドキュメントとコードの一貫性

| Acceptance Criteria | 設計での対応 | 検証方法 |
|---------------------|------------|---------|
| 4.1 新URL記載 | Phase 4: PR 本文に記載 | PR 本文確認 |
| 4.2 コミットメッセージに Issue #124 参照 | Phase 3: `Close: #124` | コミットログ確認 |
| 4.3 ステアリングドキュメント更新 | 本 design.md に記載 | `.kiro/steering/` 更新（オプション） |

### Requirement 5: 後方互換性とリグレッションの防止

| Acceptance Criteria | 設計での対応 | 検証方法 |
|---------------------|------------|---------|
| 5.1 既存ページへの影響なし | ファイル移動のみ、他ファイル変更なし | ブラウザアクセス確認 |
| 5.2 Jest テストパス | Phase 2: `npm test` | テスト結果確認 |
| 5.3 User App ビルド成功 | リグレッションテスト | User App ビルド実行 |
| 5.4 User App に同様の問題なし | （既に User App は正しい構造） | User App コード確認 |

## まとめ

### 設計の強み

1. **最小限の変更**: ファイル移動のみ、内容変更なし
2. **リスク低**: 影響範囲が1ページのみ
3. **一貫性**: 他のページと同じ i18n 構造に統一
4. **Next.js ベストプラクティス準拠**: App Router のレイアウト要件を満たす
5. **後方互換性**: 既存機能を破壊しない

### 技術的負債の解消

- ❌ **修正前**: Next.js App Router 要件違反、i18n 構造との不一致
- ✅ **修正後**: Next.js ベストプラクティス準拠、プロジェクト規約統一

### 実装時間の見積もり

| Phase | 時間 | 備考 |
|-------|-----|-----|
| Phase 1: ファイル移動 | 5分 | Git コマンド実行 |
| Phase 2: ローカル検証 | 10分 | ビルド・アクセス確認 |
| Phase 3: コミット・プッシュ | 5分 | Git 操作 |
| Phase 4: PR 作成 | 5分 | GitHub CLI 実行 |
| Phase 5: CI/CD 検証 | 自動 | GitHub Actions 自動実行 |
| **合計** | **約25分** | CI/CD 除く |

### 次のステップ

1. ✅ この design.md をレビュー
2. ⏭️ `/kiro:spec-tasks admin-app-build-fix-simple-error-test -y` を実行してタスクを生成
3. ⏭️ タスクを実行して実装
4. ⏭️ PR を作成して Issue #124 をクローズ
