<?php

declare(strict_types=1);

/**
 * パフォーマンス監視設定
 *
 * パフォーマンスメトリクス収集の設定を管理します。
 * レスポンス時間閾値、パーセンタイル設定、アラート条件を定義します。
 *
 * Requirements: 2.6, 2.7, 13.4
 */
return [
    /*
    |--------------------------------------------------------------------------
    | メトリクス設定
    |--------------------------------------------------------------------------
    |
    | パフォーマンスメトリクスの収集と監視に関する設定です。
    |
    */
    'metrics' => [
        /*
        |----------------------------------------------------------------------
        | レスポンス時間設定
        |----------------------------------------------------------------------
        |
        | APIレスポンス時間の測定とアラート設定
        | - percentiles: 測定するパーセンタイル値
        | - alert_threshold: アラート発火閾値（ミリ秒）
        |
        */
        'response_time' => [
            // パーセンタイル計算対象（p50, p90, p95, p99）
            'percentiles' => [50, 90, 95, 99],

            // アラート閾値: 200ms（0.2秒）
            // この値を超えるとパフォーマンス警告ログを記録
            'alert_threshold' => 200,
        ],
    ],
];
