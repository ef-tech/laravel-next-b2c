<?php

declare(strict_types=1);

/**
 * レート制限設定
 *
 * エンドポイントごとの動的レート制限設定を管理します。
 * DDD/クリーンアーキテクチャに基づく4種類のエンドポイント分類を使用します。
 *
 * エンドポイント分類（認証状態 × 機密性）:
 * - public_unauthenticated: 未認証 + 公開エンドポイント（一般API）
 * - protected_unauthenticated: 未認証 + 保護エンドポイント（ログイン、パスワードリセット等）
 * - public_authenticated: 認証済み + 公開エンドポイント（認証済みAPI）
 * - protected_authenticated: 認証済み + 保護エンドポイント（管理者API、決済等）
 *
 * Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6, 8.1, 8.3, 8.4, 8.5
 */
return [
    /*
    |--------------------------------------------------------------------------
    | エンドポイント分類別レート制限設定（DDD準拠）
    |--------------------------------------------------------------------------
    |
    | 4種類のエンドポイント分類に対応するレート制限設定です。
    | 環境変数で設定を上書き可能（.env.exampleを参照）
    |
    | - max_attempts: 許可するリクエスト数（環境変数: RATELIMIT_{TYPE}_MAX_ATTEMPTS）
    | - decay_minutes: レート制限の時間単位（分）（環境変数: RATELIMIT_{TYPE}_DECAY_MINUTES）
    |
    | ⚠️ 設定変更時の影響:
    | - max_attempts を増やす: サーバー負荷増加、DDoS耐性低下
    | - max_attempts を減らす: ユーザー体験悪化、正常利用者への影響
    | - decay_minutes を変更: レート制限の緩さが変わる
    |
    */
    'endpoint_types' => [
        /*
        |----------------------------------------------------------------------
        | 公開・未認証エンドポイント（public_unauthenticated）
        |----------------------------------------------------------------------
        |
        | 認証不要の一般API制限（未認証ユーザー）
        |
        | 推奨設定値: 60 req/min
        | 識別方法: IPアドレス
        |
        | 💡 設定のポイント:
        | - 未認証ユーザーの通常API利用を想定
        | - 認証済みより厳しく設定（DDoS対策）
        | - ページ表示、商品一覧取得等で使用
        |
        */
        'public_unauthenticated' => [
            'max_attempts' => (int) env('RATELIMIT_PUBLIC_UNAUTHENTICATED_MAX_ATTEMPTS', 60),
            'decay_minutes' => (int) env('RATELIMIT_PUBLIC_UNAUTHENTICATED_DECAY_MINUTES', 1),
        ],

        /*
        |----------------------------------------------------------------------
        | 保護・未認証エンドポイント（protected_unauthenticated）
        |----------------------------------------------------------------------
        |
        | 未認証の機密エンドポイント制限（ログイン、パスワードリセット等）
        |
        | 推奨設定値: 5 req/10min
        | 識別方法: IPアドレス + Emailアドレス（SHA-256ハッシュ化）
        |
        | 💡 設定のポイント:
        | - ブルートフォース攻撃対策のため最も厳格な制限
        | - ログイン、パスワードリセット、アカウント登録で使用
        | - IP + Email の組み合わせで識別（同一IPから異なるEmailへの攻撃を防ぐ）
        |
        */
        'protected_unauthenticated' => [
            'max_attempts' => (int) env('RATELIMIT_PROTECTED_UNAUTHENTICATED_MAX_ATTEMPTS', 5),
            'decay_minutes' => (int) env('RATELIMIT_PROTECTED_UNAUTHENTICATED_DECAY_MINUTES', 10),
        ],

        /*
        |----------------------------------------------------------------------
        | 公開・認証済みエンドポイント（public_authenticated）
        |----------------------------------------------------------------------
        |
        | 認証済みユーザーの一般API制限
        |
        | 推奨設定値: 120 req/min
        | 識別方法: User ID（フォールバック: Token ID → IPアドレス）
        |
        | 💡 設定のポイント:
        | - 認証済みユーザーは信頼性が高いため制限を緩和
        | - リアルタイムアプリケーション（チャット等）でも使用可能
        | - User ID単位で制限（複数デバイスからのアクセスを1ユーザーとして集約）
        |
        */
        'public_authenticated' => [
            'max_attempts' => (int) env('RATELIMIT_PUBLIC_AUTHENTICATED_MAX_ATTEMPTS', 120),
            'decay_minutes' => (int) env('RATELIMIT_PUBLIC_AUTHENTICATED_DECAY_MINUTES', 1),
        ],

        /*
        |----------------------------------------------------------------------
        | 保護・認証済みエンドポイント（protected_authenticated）
        |----------------------------------------------------------------------
        |
        | 認証済みユーザーの機密API制限（管理者API、決済等）
        |
        | 推奨設定値: 30 req/min
        | 識別方法: User ID（フォールバック: Token ID → IPアドレス）
        |
        | 💡 設定のポイント:
        | - 管理者操作、決済、機密データ操作で使用
        | - 認証済みでも厳格な制限により不正操作リスクを最小化
        | - ユーザー削除、権限変更、大量データ操作等に適用
        |
        */
        'protected_authenticated' => [
            'max_attempts' => (int) env('RATELIMIT_PROTECTED_AUTHENTICATED_MAX_ATTEMPTS', 30),
            'decay_minutes' => (int) env('RATELIMIT_PROTECTED_AUTHENTICATED_DECAY_MINUTES', 1),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | デフォルトレート制限設定
    |--------------------------------------------------------------------------
    |
    | エンドポイント分類が不明な場合のフォールバック設定です。
    | 最も厳格な制限を適用してセキュリティを確保します。
    |
    */
    'default' => [
        'max_attempts' => (int) env('RATELIMIT_DEFAULT_MAX_ATTEMPTS', 30),
        'decay_minutes' => (int) env('RATELIMIT_DEFAULT_DECAY_MINUTES', 1),
    ],

    /*
    |--------------------------------------------------------------------------
    | 保護ルートパターン設定
    |--------------------------------------------------------------------------
    |
    | 保護エンドポイント判定に使用するルート名パターンです。
    | Laravelのルート名とワイルドカードパターン（*）でマッチングします。
    |
    | 例:
    | - 'login' → ルート名が完全一致
    | - 'password.*' → 'password.email', 'password.reset' 等にマッチ
    | - 'admin.*' → 'admin.users.index', 'admin.settings.update' 等にマッチ
    |
    */
    'protected_routes' => [
        'login',           // ログイン
        'register',        // ユーザー登録
        'password.*',      // パスワードリセット関連
        'admin.*',         // 管理者API全般
        'payment.*',       // 決済API全般
    ],

    /*
    |--------------------------------------------------------------------------
    | レガシーエンドポイント設定（後方互換性）
    |--------------------------------------------------------------------------
    |
    | 既存のエンドポイント設定です。
    | 新規実装では endpoint_types を使用してください。
    |
    | ⚠️ 非推奨: この設定は将来のバージョンで削除される予定です。
    |
    */
    'endpoints' => [
        /*
        |----------------------------------------------------------------------
        | APIエンドポイント（認証済みユーザー）
        |----------------------------------------------------------------------
        |
        | 認証されたユーザーのAPI利用制限
        |
        | 推奨設定値: 60-120 req/min
        | 識別方法: ip（デフォルト）または user（認証済み）
        |
        | 💡 設定のポイント:
        | - 通常のAPI利用では60 req/minで十分（1秒に1リクエスト）
        | - リアルタイムアプリケーション（チャット等）は120 req/min推奨
        | - 'by' を 'user' にするとユーザーID単位で制限可能
        |
        */
        'api' => [
            'requests' => (int) env('RATELIMIT_API_REQUESTS', 60),
            'per_minute' => (int) env('RATELIMIT_API_PER_MINUTE', 1),
            'by' => env('RATELIMIT_API_BY', 'ip'),
        ],

        /*
        |----------------------------------------------------------------------
        | Publicエンドポイント（未認証ユーザー）
        |----------------------------------------------------------------------
        |
        | 認証不要の公開API制限
        |
        | 推奨設定値: 30-60 req/min
        | 識別方法: ip（IPアドレス単位）
        |
        | 💡 設定のポイント:
        | - 認証済みより厳しく設定（DDoS対策）
        | - ログイン前のページ表示・登録フォーム等で使用
        | - 'by' は必ず 'ip' を使用（未認証のためユーザー識別不可）
        |
        */
        'public' => [
            'requests' => (int) env('RATELIMIT_PUBLIC_REQUESTS', 30),
            'per_minute' => (int) env('RATELIMIT_PUBLIC_PER_MINUTE', 1),
            'by' => env('RATELIMIT_PUBLIC_BY', 'ip'),
        ],

        /*
        |----------------------------------------------------------------------
        | Webhookエンドポイント（外部サービス連携）
        |----------------------------------------------------------------------
        |
        | 外部サービスからのWebhook受信制限
        |
        | 推奨設定値: 100-200 req/min
        | 識別方法: ip（送信元IPアドレス単位）
        |
        | 💡 設定のポイント:
        | - Stripe, GitHub等のWebhook受信で使用
        | - 外部サービスのリトライ挙動を考慮して高めに設定
        | - 特定サービスが高頻度の場合は個別に調整
        |
        */
        'webhook' => [
            'requests' => (int) env('RATELIMIT_WEBHOOK_REQUESTS', 100),
            'per_minute' => (int) env('RATELIMIT_WEBHOOK_PER_MINUTE', 1),
            'by' => env('RATELIMIT_WEBHOOK_BY', 'ip'),
        ],

        /*
        |----------------------------------------------------------------------
        | Strictエンドポイント（管理者API・機密操作）
        |----------------------------------------------------------------------
        |
        | サーバー間通信や管理者用API制限
        |
        | 推奨設定値: 10-20 req/min
        | 識別方法: user（ユーザーID単位）
        |
        | 💡 設定のポイント:
        | - 機密性の高い操作（ユーザー削除、権限変更等）で使用
        | - 厳しい制限により不正操作リスクを最小化
        | - 'by' を 'user' にして認証済みユーザー単位で制限
        |
        */
        'strict' => [
            'requests' => (int) env('RATELIMIT_STRICT_REQUESTS', 10),
            'per_minute' => (int) env('RATELIMIT_STRICT_PER_MINUTE', 1),
            'by' => env('RATELIMIT_STRICT_BY', 'user'),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | キャッシュストア設定
    |--------------------------------------------------------------------------
    |
    | レート制限カウンターの保存先設定です。
    | Redisを使用して高速な分散カウントを実現します。
    |
    | テスト環境では RATELIMIT_CACHE_STORE=array を設定することで、
    | インメモリキャッシュを使用してテスト間の干渉を防ぐことができます。
    |
    */
    'cache' => [
        'store' => env('RATELIMIT_CACHE_STORE', 'redis'),
        'prefix' => 'rate_limit',
    ],
];
